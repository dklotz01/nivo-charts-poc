{
  "name": "project-root",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd frontend && yarn run dev\" \"cd backend && node server.js\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
